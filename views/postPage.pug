extends layouts/main-layout.pug

block content 
    script. 
        var postId = '!{postId}'
    .postContainer
    
    +createReplayModal(userLoggedIn)    

block scripts
    script(src="/js/postPage.js")